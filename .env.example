# Local development (SQLite)
DATABASE_URL="file:./dev.db"

# Production (Neon PostgreSQL) — use this when deploying:
# DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Generate with: openssl rand -base64 32  (or see DEPLOYMENT.md)
AUTH_SECRET="change-this-to-a-random-secret-in-production"

# Local:
AUTH_URL="http://localhost:3000"
# Production (must match your Vercel URL exactly):
# AUTH_URL="https://your-app.vercel.app"
